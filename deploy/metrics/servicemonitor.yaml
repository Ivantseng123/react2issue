apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: agentdock
  labels:
    app: agentdock
spec:
  selector:
    matchLabels:
      app: agentdock
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
